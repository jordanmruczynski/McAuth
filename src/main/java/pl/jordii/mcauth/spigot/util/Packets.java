package pl.jordii.mcauth.spigot.util;

import org.bukkit.entity.Player;

public class Packets {
//    public static void sendActionBar(Player player, String message) {
//        CraftPlayer p = (CraftPlayer) player;
//        IChatBaseComponent cbc = IChatBaseComponent.ChatSerializer.a("{\"text\": \"" + message + "\"}");
//        PacketPlayOutChat ppoc = new PacketPlayOutChat(cbc, (byte) 2);
//        p.getHandle().playerConnection.sendPacket(ppoc);
//    }
//    //create method to send title to player
//    public static void sendTitle(Player player, String title, String subtitle, int fadeIn, int stay, int fadeOut) {
//        CraftPlayer p = (CraftPlayer) player;
//        PacketPlayOutTitle packetPlayOutTitle = new PacketPlayOutTitle(PacketPlayOutTitle.EnumTitleAction.TITLE, IChatBaseComponent.ChatSerializer.a("{\"text\": \"" + title + "\"}"), fadeIn, stay, fadeOut);
//        PacketPlayOutTitle packetPlayOutSubTitle = new PacketPlayOutTitle(PacketPlayOutTitle.EnumTitleAction.SUBTITLE, IChatBaseComponent.ChatSerializer.a("{\"text\": \"" + subtitle + "\"}"), fadeIn, stay, fadeOut);
//        p.getHandle().playerConnection.sendPacket(packetPlayOutTitle);
//        p.getHandle().playerConnection.sendPacket(packetPlayOutSubTitle);
//    }

    //create method to make player invisible for everyone



}
